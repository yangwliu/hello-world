pipeline {
    agent any

    stages {
        stage('clone') {
            steps {
                sh echo 'clone'
            }
        }

        stage('Unit test') {
            steps {
                sh echo 'unit test'
            }
        }

    }
}
